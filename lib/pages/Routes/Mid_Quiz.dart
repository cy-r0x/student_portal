import 'package:flutter/material.dart';

class MidQuiz extends StatelessWidget {
  const MidQuiz({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "THis is MidQuiz Section",
        style: TextStyle(fontSize: 26),
      ),
    );
  }
}
